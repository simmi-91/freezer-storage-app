# Copy to .env.local for local dev overrides (gitignored via *.local)
# .env holds production/shared defaults and is committed
GEMINI_API_KEY=your_key_here
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=freezer_storage
FRONTEND_ORIGINS=https://3lin.no,https://www.3lin.no